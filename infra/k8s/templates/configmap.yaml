apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "andy-wildlife-ui.fullname" . }}-nginx-conf
  labels:
    {{- include "andy-wildlife-ui.labels" . | nindent 4 }}
data:
  nginx.conf: |
{{ .Files.Get "nginx.conf" | indent 4 }}
